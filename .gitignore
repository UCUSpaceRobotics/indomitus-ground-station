__pycache__/
*.pyc
install/
build/
log/

# Generated per machine by docker/gen-dds-profile.sh on every container start.
docker/fastdds_rover_link.xml

# Machine-local compose overrides (board serial ports, rover addresses).
# `.env.example` is the tracked one.
.env
